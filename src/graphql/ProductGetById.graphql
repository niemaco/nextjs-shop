query ProductGetById($productId: ID!) {
    product(id: $productId) {
        categories {
            name
            slug
        }
        description
        id
        images {
            url
            width
            height
            alt
        }
        name
        price
        slug
    }
}
