query ProductsGet($offset: Int, $take: Int) {
    products(
        order: ASC,
        orderBy: DEFAULT,
        skip: $offset,
        take: $take
    ) {
        data {
            ...ProductItem
        }
        meta {
            total
        }
    }
}
