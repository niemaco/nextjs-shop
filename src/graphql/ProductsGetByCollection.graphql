query ProductsGetByCollection($slug: String!) {
    collection(slug: $slug) {
        products {
            id
            name
            price
            slug
            categories {
                name
                slug
            }
            images {
                url
            }
        }
    }
}
